<Window x:Class="RTS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RTS"
        xmlns:controls="clr-namespace:RTS.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Welcome" FontSize="22" FontFamily="Segoe UI Variable Display Semibold" LineHeight="22" Padding="10" />
        <controls:NavigationBar Grid.Row="1"/>
      
        <ScrollViewer Grid.Row="2">
            <ContentControl Content="{Binding CurrentViewModel}" ScrollViewer.CanContentScroll="True" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
        </ScrollViewer>
    </Grid>
</Window>
