<Application x:Class="RTS.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RTS"
             xmlns:viewmodels="clr-namespace:RTS.ViewModels"
             xmlns:views="clr-namespace:RTS.Views">
    <Application.Resources>
        <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:VacancyListViewModel}">
            <views:VacancyListView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:ApplicationListViewModel}">
            <views:ApplicationListView />
        </DataTemplate>


        <DataTemplate DataType="{x:Type viewmodels:CandidateListViewModel}">
            <views:CandidateListView />
        </DataTemplate>


    </Application.Resources>
</Application>